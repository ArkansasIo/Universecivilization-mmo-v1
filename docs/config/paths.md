# Config — Paths

**File:** `src/config/paths.ts`

## Purpose
Asset and data path configuration — maps symbolic paths to actual file locations.

## Key Exports
- `PATHS` — Object with `assets` (root), `images`, `audio`, `data`, `save`, `logs`, `config`, `mods`, and `screenshots` subpaths used throughout the application for resource loading.
